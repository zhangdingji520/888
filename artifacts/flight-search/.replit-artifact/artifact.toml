kind = "web"
previewPath = "/"
title = "Flight Search"
version = "1.0.0"
id = "artifacts/flight-search"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19598

[services.development]
run = "pnpm --filter @workspace/flight-search run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/flight-search", "run", "build" ]
publicDir = "artifacts/flight-search/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19598"
BASE_PATH = "/"
